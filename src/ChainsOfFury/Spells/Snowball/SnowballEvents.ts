/**
 * A set of events for the ice mirror.
 */

export const SnowballEvents = {
    /** Requires {id: number}
     * where id is the id of the snowball to despawn
     */
    DESPAWN_SNOWBALL: "DESPAWN_SNOWBALL"
}